export class CityUS {

  city: string;
  zip: string;
  state: string;

  constructor(city: string, zip: string, state: string) {
    this.city = city;
    this.zip = zip;
    this.state = state;
  }

}
